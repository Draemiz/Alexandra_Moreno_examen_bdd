info:
  name: Buscar por ID
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/vuelos/4
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
